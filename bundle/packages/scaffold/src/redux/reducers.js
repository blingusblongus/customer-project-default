import { reducer as app } from './modules/app';
import { forms } from './modules/formsReducer';
//import sampleReducer from './modules/sample';

export default {
  app,
  forms, // example reducer (already included in app)
  // sample: sampleReducer,
};
